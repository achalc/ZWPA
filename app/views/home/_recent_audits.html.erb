<table class="table">
  <tr>
    <th width="200">Client</th>
    <th width="120">Generator</th>
    <th width="120">Start Date</th>
    <th width="120">End Date</th>
    <th></th>
  </tr>

  <% audits.each do |audit| %>
    <tr>
      <td><%= link_to audit.request.customer.company_name, customers_path(audit.request.customer) %></td>
      <td><%= audit.generator%></td>
      <td><%= audit.start_date.strftime('%b %e, %Y')%></td>
      <td><%= audit.end_date.strftime('%b %e, %Y')%></td>
      <td><%= link_to '>', audits_path(audit)%></td>
      
      
    </tr>
  <% end %>
</table></br>

<%= link_to "View All Audits", audits_path %>

<!-- Add the pagination using will_paginate gem -->
<!-- <span class="paginate"> -->
<%#= will_paginate requests, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
<!-- </span> -->
